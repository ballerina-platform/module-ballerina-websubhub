---
apiVersion: "v1"
kind: "Service"
metadata:
  labels:
    app: "in_memory_hub"
  name: "in-memory-hub-svc"
spec:
  ports:
    - name: "port-1-in-mem"
      port: 9090
      protocol: "TCP"
      targetPort: 9090
  selector:
    app: "in_memory_hub"
  type: "ClusterIP"
